# Notebooks

This project is currently implemented as a Python package and executable pipeline using `main.py`.

A notebook version can be added later for walkthrough-style explanation, but the main reproducible workflow is:

```bash
python main.py
```
